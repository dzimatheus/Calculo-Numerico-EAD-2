clc;
clear;

// Carrega o método
exec('C:\Users\dziob\OneDrive\Área de Trabalho\Calculo Numerico\Programas/iteracaoLinear.sci');

// Função original
deff('y=f(x)', 'y=x^3 - x + 1');

// Função de iteração
// x³ - x + 1 = 0
// x = sqrt^3(x - 1)

deff('y=phi(x)', 'y=sign(x-1)*abs(x-1)^(1/3)');

// Dados
x0 = -1.5;
tol = 1e-3;
maxIter = 100;

// Tempo inicial
t1 = getdate();

// Executa método
[x, iter] = iteracaoLinear(phi, f, x0, tol, maxIter);

// Tempo final
t2 = getdate();

disp("Iteração Linear");
disp("Raiz aproximada:");
disp(x);

disp("Número de iterações:");
disp(iter);
