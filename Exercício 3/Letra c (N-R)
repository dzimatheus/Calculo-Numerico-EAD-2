clc;
clear;

exec('C:\Users\dziob\OneDrive\Área de Trabalho\Calculo Numerico\Programas\newtonRaphson.sci');

// Função
deff('y=f(x)', 'y=exp(x/2)-x^3');

// Derivada
deff('y=df(x)', 'y=(1/2)*exp(x/2)-3*x^2');

// Dados
x0 = 1.2;
tol = 1e-3;
maxIter = 100;

// Executa
[x, iter] = newtonRaphson(f, df, x0, tol, maxIter);

disp("Newton-Raphson");
disp("Raiz aproximada:");
disp(x);

disp("Número de iterações:");
disp(iter);
