clc;
clear;

exec('C:\Users\dziob\OneDrive\Área de Trabalho\Calculo Numerico\Programas\newtonRaphson.sci');

// Função
deff('y=f(x)', 'y=x^2 + 3*x - cos(x) - 2.45');

// Derivada
deff('y=df(x)', 'y=2*x + 3 + sin(x)');

// Dados
x0 = 0.7;
tol = 1e-3;
maxIter = 100;

// Executa
[x, iter] = newtonRaphson(f, df, x0, tol, maxIter);

disp("Newton-Raphson");
disp(x);
disp(iter);
